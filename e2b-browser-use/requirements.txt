# E2B browser sandbox lifecycle and browsertool CDP endpoint access.
e2b>=2.0.0,<2.25.0

# Verified with BrowserUse 0.13.x: Browser supports remote CDP headers and
# is_local=False, which are required for E2B browsertool connections.
browser-use>=0.13.4,<0.14.0

# Direct imports used by config/settings.py.
python-dotenv>=1.0.0,<2.0.0
pydantic>=2.0.0,<3.0.0

# Used directly for the authenticated browsertool CDP readiness probe.
playwright>=1.49.0,<2.0.0
